// consume.js
let input = '';

process.stdin.setEncoding('utf8');

process.stdin.on('data', chunk => {
  input += chunk;
  console.log("recived");
});

process.stdin.on('end', () => {
  console.log("Here is the data: " + input.trim());
});
